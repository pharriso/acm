apiVersion: policy.open-cluster-management.io/v1
kind: PolicyGenerator
metadata:
  name: nmstate-operator
policyDefaults:
  namespace: default
  severity: high
  remediationAction: enforce
  complianceType: musthave
policies:
  - name: nmstate-operator-policy
    manifests:
      - path: manifests
